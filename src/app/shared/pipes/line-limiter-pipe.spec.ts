import { LineLimiterPipe } from './line-limiter-pipe';

describe('LineLimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new LineLimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
