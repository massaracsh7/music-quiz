import { ResultColor } from './result-color';

describe('ResultColor', () => {
  it('should create an instance', () => {
    const directive = new ResultColor();
    expect(directive).toBeTruthy();
  });
});
