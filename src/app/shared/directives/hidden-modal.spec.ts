import { HiddenModal } from './hidden-modal';

describe('HiddenModal', () => {
  it('should create an instance', () => {
    const directive = new HiddenModal();
    expect(directive).toBeTruthy();
  });
});
