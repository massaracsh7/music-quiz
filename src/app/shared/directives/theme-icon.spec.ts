import { ThemeIcon } from './theme-icon';

describe('ThemeIcon', () => {
  it('should create an instance', () => {
    const directive = new ThemeIcon();
    expect(directive).toBeTruthy();
  });
});
